{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@stitches/react": "^1.2.8",
    "polished": "^4.2.2"
  },

  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  
  "devDependencies": {
    "turbo": "^1.6.3"
  }
}
